# تقييم المشروع — 2026-08-29

**المُقيِّم:** ZCode · **الفرع:** `refactor/comments-explanatory-only` @ 5b07b39
**المنهجية:** مبني على المراجعة العميقة 2026-08-28 (`DEEP_CODE_REVIEW_2026-08-28.md`) + **تحقق تجريبي جديد اليوم**: تشغيل الحزم الثلاث كاملة، build إنتاجي وقياس فعلي لحجم التحميل الأولي، فحص advisors للقاعدة الحية، وفحص حالة الـ git/CI.

---

## الملخص التنفيذي

من مراجعة مبارح (code 9/10 · جاهزية إنتاج 6.5/10) لليوم، انعملت **10 جولات إصلاح متتالية** غلقت تقريباً كل ما كان مفتوحاً: CI صار مفروضاً على master، فجوة RLS بالهجرات انسدت (033–035)، `agent/turn` أخذ Zod + idempotency، انبنيت 4 طبقات E2E، انعمل أول اختبار حمل حقيقي بمشروع (24/24 عبر OpenRouter)، انضافت نسخ احتياطية يومية + runbook إنتاجي، والتحميل الأولي نزل **3,744KB → 1,821KB (−51%)**.

التحقق التجريبي اليوم أكّد أغلب الادعاءات — **لكن كشف مشكلتين حقيقيتين**:

1. **حزمة اختبارات الفرونت حمراء على HEAD**: ‏5 من 556 اختبار فاشل. كسرها تحميل صفحة الـ auth الكسول (27fc1c1) — اختبارات LoginPage بتفحص بشكل متزامن والواجهة كلها صارت جوّا `React.lazy` مع `fallback={null}`. أثناء جولات الـ perf انعاد تشغيل Playwright فقط (بيستنى الـ chunk طبيعياً) — **vitest ما انعاد**.
2. **كل شغل التقوية لسا عالجهاز**: الفرع متقدّم **49 commit** عن origin وmaster متقدّم 3. يعني CI، سيرعمل النسخ الاحتياطي اليومي، وحاجز الاعتماديات عالية الخطورة — **كله معطّل فعلياً على GitHub** لأنه ما وصل لهون.

**Verdict:** الكود بأفضل حالة بتاريخه والمعمارية سليمة. النقطة الضعيفة الوحيدة المتبقية هي **العملية** (نفس تشخيص مبارح): شغل ممتاز غير مرفوع + حزمة اختبارات انكسرت بصمت. بعدها البند الوحيد المفتوح جوهرياً هو مفتاح مزود مدفوع (قرار مالي).

| المحور | 2026-08-28 | 2026-08-29 | ملاحظة |
|---|---|---|---|
| جودة الكود | 9/10 | **9/10** | ثابتة — ما انكسر شي معماري |
| الاختبارات | 9→10/10 | **9/10** | حزمة فرونت حمراء (5 فشل) |
| Infra/تشغيل | 6→8.5/10 | **8.5/10 بالكود، ~5 فعلياً** | غير مفعّل حتى يُرفع |
| جاهزية إنتاج | 6.5/10 | **~7.5/10** | beta-ready بشرطَي الرفع + إصلاح الاختبارات |

---

## يلي تغيّر — والتحقق التجريبي لكل ادعاء

| الادعاء بالسجل | تحقّقي اليوم |
|---|---|
| backend 768/768 | ✅ ‏768/768 عند التشغيل المنفرد (تشغيلة متوازية واحدة أظهرت flake — انظر Low) |
| frontend 556/556 | ❌ **551/556 — 5 فشل حقيقي على HEAD** (تفصيل أدناه) |
| pdf-processor 106/106 | ✅ ‏106/106 (10.5s) |
| typechecks نظيفة | ✅ كلاهما (جزء من نفس التشغيلات) |
| eager JS ‏1,821KB | ✅ بالضبط 1,821KB قياساً من `dist/index.html` (scripts + modulepreload) · ‏551KB مضغوطاً |
| `vite build` ناجح | ✅ (1m14s) |
| CI triggers → master | ✅ ‏`.github/workflows/ci.yml:4-8` — لكن **غير مطبّق حتى يُرفع** |
| نسخ احتياطي يومي + runbook | ✅ ‏`backup.yml` سليم (cron يومي + dispatch + إجراء استعادة) — **غير مفعّل حتى يُرفع ويضبط secret** |
| القاعدة الحية آمنة | ✅ ‏advisors اليوم: لا ثغرات جديدة — نفس المُسلّمات المُسلّفة (extensions بالـ public، leaked-password محجوز للخطة) + ‏4 جداول شغل بلا سياسات (fail-closed مقصود، INFO) |

**النتيجة:** جولات الإصلاح العشر حقيقية وموثّقة، مش paper work. الاستثناء الوحيد هو ادعاء 556/556 الفرونت.

---

## الفجوات — بالأولوية

### 1. High — حزمة الفرونت حمراء: 5 اختبارات LoginPage فاشلة
- **السبب الجذري:** ‏`frontend/src/components/LoginPage.tsx:5-7,54` — كامل واجهة الـ auth صارت chunk كسول (`SignupPage` عبر `React.lazy`) تحت `<Suspense fallback={null}>` (من 27fc1c1، الهدف تخفيف الحزمة — هدفه صحيح). الاختبارات تفحص مباشرة بعد render:
  - `frontend/src/__tests__/components/GuestLoginRouting.test.tsx:100-118` — ‏3 اختبارات (getByText متزامن → body فاضي)
  - `frontend/src/__tests__/components/LoginPageAuthFlow.test.tsx` — ‏اختبارا تدفق الـ auth
- **ليش مهم:** الادعاء المسجّل «556/556» صار غير صحيح على HEAD — وحدا ما انتبه لأن vitest ما انعاد تشغيله بعد جولات الـ perf. ‏CI كان سيلقطها فوراً… لو كانت الـ commits مرفوعة (بند 2).
- **الإصلاح (S):** استبدل `getByText` المتزامنة بـ `await findByText` / `waitFor` بالاختبارات الخمسة (الحدّ الفعلي هو تحوّل الصفحة لـ async). بديل أسلوبي: الرفض المتزامن مقبول لأن Playwright يغطي UX الحقيقي — بس الاختبارات لازم تعبّر عن الواقع الجديد.

### 2. High (عملية) — 49 commit غير مرفوعة: كل آليات الحماية معطّلة فعلياً
- `refactor/comments-explanatory-only` ‏[ahead 49] و`master` ‏[ahead 3] عن origin.
- CI (4 jobs)، النسخ الاحتياطي اليومي، حاجز الاعتماديات، وإجراء الاستعادة — كلها **نصوص على الجهاز فقط**. ‏Supabase Free **بدون أي نسخة احتياطية** حتى يشتغل الـ workflow، والـ `SUPABASE_DB_URL` secret بعد ما انضبط.
- **الإصلاح (S):** ارفع الفرع + ادمج/ارفع master → راقب أول run أخضر → شغّل `backup.yml` يدوياً مرة للتحقق → انتبه لفشل الـ 5 اختبارات الفرونت (بند 1 لازم يتصلح قبل الدمج وإلا CI أحمر).

### 3. Medium — مفاتيح API حية بنص صريح بمجلد المشروع
- `switch-provider.ps1` (غير متبّع — منيح) يحوي مفتاحي Nous Research وNVIDIA **صالحين بنص الملف**، ومجلد المشروع متزامن مع OneDrive. الملفان أدوات شخصية لتبديل مزوّد أداة البرمجة، مش من المشروع أصلاً.
- **الإصلاح (S):** انقل الملفين خارج مجلد المشروع (مثلاً `%USERPROFILE%\bin`) وأضفهما لـ `.gitignore` احتياطاً، ولفّ المفاتيح إذا كانت حساسة. المسح التاريخي للسcrets (جولة رابعة) نضيف — هاد خطر مستقبلي فقط.

### 4. Low — flake خلفي تحت التشغيل المتوازي
- تشغيلة واحدة (باك + فرونت متزامنين على نفس الجهاز) فشلت باختبارَي metadata أدوات الـ calendar («marks every calendar tool as requiring a userId») — تسجيل الأدوات عبر `import()` ديناميكي حساس للتحميل تحت ضغط CPU. تشغيلتان منفردتان: 768/768.
- **الإصلاح (S):** تأكيد تسجيل الـ metadata قبل الـ assertion (await على import صريح أو حارس readiness). غير عائق — ‏CI يشغّل كل job على آلة منفصلة.

### 5. مفتوح (قرارات، مش كود)
- **مفتاح مزود مدفوع** (Groq/OpenRouter): السقف الحقيقي الوحيد على الحجم — كل شي تاني جاهز. بعده أعِد الحمل بـ `LOAD_VUS=50`.
- **Leaked-password protection**: محجوز لخطة Supabase Pro (402) — تعويض مطبّق (min length 8).
- **بند الصوت** (من سجل الصوت): شخصية الوكيل لسا الـ template الافتراضي، أوزان ONNX وshimmer مؤجّلة، شرط capacity ~50 رسالة.
- **تقسيم ملفات الواجهة العملاقة** (ChatHistoryContext 728، useChatRuntime 873، MessageComponents 943): تدريجي، غير عائق.

---

## خطة العمل المقترحة (بهذا الترتيب)

| # | الإجراء | الجهد | ليش أولاً |
|---|---|---|---|
| 1 | إصلاح اختبارات LoginPage الخمسة (findBy/waitFor) | S | حتى تبقى الحزمة صادقة قبل أي رفع |
| 2 | رفع الفرع + master → CI أخضر → تفعيل النسخ الاحتياطي (dispatch يدوي) | S | كل حماية البنى لسا نظرية |
| 3 | نقل switch-provider خارج المشروع + .gitignore | S | إغلاق ثغرة secrets |
| 4 | تثبيت flake اختبار الـ calendar | S | ثقة بالحزمة تحت أي ظرف |
| 5 | مفتاح مزود مدفوع (قرار شراء) + إعادة اختبار حمل أكبر | M | رفع سقف الحجم الفعلي |
| 6 | (لاحقاً) شخصية الوكيل الصوتي، ONNX/shimmer، تقسيم ملفات الواجهة | M | جودة، مش عوائق |

---

## تغطية التقييم (بشفافية)

**عميق اليوم:** حالة الـ git/الرفع، الحزم الثلاث كاملة تجريبياً، build + قياس التحميل الأولي، workflows (CI/backup)، advisors للقاعدة الحية، تشخيص فشل اختبارات الفرونت حتى السبب الجذري بالسطر.
**معتمد على مراجعة 08-28 بدون إعادة قراءة:** المعمارية الداخلية (routes→pipeline→services)، RAG/tools/memory، ملفات الواجهة العملاقة — ما تغيّر منها إلا ما وثّقته جولات الإصلاح، وتحقيقات مبارح التجريبية (44/44 RLS، anon-CRUD مغلق) ما زالت مرجعاً.
**ما نُفّذ:** لا تعديلات كود — هذا تقرير فقط.

---

## ملحق

- **جداول الشغل بلا سياسات RLS** (`documents`, `memory_extraction_jobs/dead_letter`, `message_embeddings`): RLS مفعّل وبدون سياسات = deny-all لـ anon/authenticated — اتجاه آمن (fail-closed) لأن الوصول يتم بـ service role فقط. لمّحة توثيق بالـ migrations README تكفي.
- **فرق قياس الضغط:** قياسي gzip (مستوى 9) أعطى 551KB للـ eager مقابل ~470KB المقدّرة بالسجل (nginx gzip level 6 أقل كفاءة قليلاً) — لا تضارب فعلي.
- **مراجع الإصلاح:** ‏`DEEP_CODE_REVIEW_2026-08-28.md` (المراجعة الأصلية + سجل الـ 10 جولات) · ‏`OPERATIONS.md` (runbook) · ‏`backend/scripts/load-guest.mjs` (اختبار الحمل قابل لإعادة الاستخدام).
